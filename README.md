### Instructions:

- Install virtualenv
- run pip install -r requirements.txt


### To run program:

source venv/bin/activate
cd project
python main.py


### to run unittest

pytest -v --cov=project --cov-report=html
